name: fitness_repository
description: A repository that manages, fitness activity and so on

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  cloud_firestore: ^2.1.0
  equatable: ^2.0.0
  firebase_auth: ^1.1.4
  flutter:
    sdk: flutter
  hive: ^2.0.4
  hive_flutter: ^1.1.0
  intl: ^0.17.0
